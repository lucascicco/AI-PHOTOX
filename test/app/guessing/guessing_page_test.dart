import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Aiphotox/app/modules/guessing/guessing_page.dart';

void main() {
  testWidgets('GuessingPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GuessingPage(title: 'Guessing')));
    //  final titleFinder = find.text('Guessing');
    //  expect(titleFinder, findsOneWidget);
  });
}
