import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Aiphotox/app/modules/guessing/guessing_controller.dart';
import 'package:Aiphotox/app/modules/guessing/guessing_module.dart';

void main() {
  initModule(GuessingModule());
  // GuessingController guessing;
  //
  setUp(() {
    //     guessing = GuessingModule.to.get<GuessingController>();
  });

  group('GuessingController Test', () {
    //   test("First Test", () {
    //     expect(guessing, isInstanceOf<GuessingController>());
    //   });

    //   test("Set Value", () {
    //     expect(guessing.value, equals(0));
    //     guessing.increment();
    //     expect(guessing.value, equals(1));
    //   });
  });
}
